// Production defaults. Not yet live — the Caddy site blocks for these domains don't exist yet
// (see PLAN.md#deployment) — but the shape is correct for when they do.
export const environment = {
  apiBaseUrl: 'https://api.yeppbot.com',
  frontendBaseUrl: 'https://dash.yeppbot.com',
};
